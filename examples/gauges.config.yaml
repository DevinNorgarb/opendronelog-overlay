video:
  # Panel top-left in output frame pixels.
  x: 28
  y: 28
  width: 260
  row_height: 30
  opacity: 0.58
  corner_radius: 14

style:
  # Hex colors for panel and text.
  panel_bg_hex: "#1E2434"
  label_text_hex: "#C8CDDC"
  value_text_hex: "#EFF3F8"
  muted_text_hex: "#AAB2C2"

transparent_output:
  width: 3840  # 3840x2160 4k resolution
  height: 2160  # 3840x2160 4k resolution
  fps: 24  # 24fps is standard for 4k video
  duration_pad_s: 0.0
  # png or qtrle (both keep alpha in .mov). png is usually safer in preview apps.
  codec: png

telemetry:
  # Pick only the fields you want in the card.
  include:
    - height
    - speed
    - distance_to_home
    - battery
    - satellites
    - lat
    - lng
    - flight_mode

  # Optional per-field display labels.
  labels:
    height: Height
    speed: Speed
    distance_to_home: Dist. Home
    battery: Battery
    satellites: Satellites
    lat: Lat
    lng: Lng
    flight_mode: Mode

  # Decimals for numeric fields.
  decimals:
    height: 1
    speed: 1
    distance_to_home: 0
    battery: 0
    satellites: 0
    lat: 6
    lng: 6

  # auto = keep source unit family from CSV, metric/imperial force output units.
  unit_system: auto

rc_sticks:
  enabled: true
  title: RC STICKS
  size: 54
  gap: 12

gauges:
  enabled: true
  layout: horizontal       # horizontal or vertical
  width: 140
  height: 140
  x: -1                    # -1 = auto-position to right of text panel
  y: 28
  gap: 14
  arc_color_hex: "#2D3446"
  needle_color_hex: "#28C7EC"
  tick_color_hex: "#6B7280"
  label_color_hex: "#C8CDDC"
  value_color_hex: "#EFF3F8"
